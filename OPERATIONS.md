# 与那国語オンライン辞書 v2 運用手順

## 人間が最初に行うこと

コードでは決められないため、次の入力だけは完全管理者が行います。

1. 完全管理者本人のユーザー名・表示名・12文字以上のパスワードを決める。
2. 残り2人の氏名とユーザー名を確認する。
3. 隔離された42件を原資料と照合し、復元・再入力・保留を判断する。
4. 本番ドメイン、HTTPS証明書、公開切替日時を決める。

## 初回セットアップ

```bash
cp .env.example .env
# .env の秘密鍵、DB絶対パス、ドメインを実環境に合わせる
./v6/venv/bin/python scripts/upgrade_v2.py
./v6/venv/bin/python scripts/manage_users.py owner "完全管理者の表示名" --role admin
./v6/venv/bin/python scripts/rebuild_search.py
```

秘密鍵とパスワードはGit、チャット、操作マニュアルへ記載しません。

完全管理者がログイン後、「メンバー・割り当て・バックアップ」から残り2人の
個別アカウントを発行します。初期パスワードは本人へ別経路で渡します。本人は
初回ログイン時に自分専用のパスワードへ変更します。

## 既存語彙の点検開始

1. 3アカウントが有効なことを確認する。
2. 完全管理者メニューで「未割り当て語彙を自動配分」を押す。
3. 各自のダッシュボードに担当語彙が表示されることを確認する。
4. 修正なしなら全チェック項目を確認し「修正なしで点検完了」。
5. 修正した場合は下書きを保存し、本人以外へ相互確認を依頼する。

自動配分は例文数、意味数、欠落項目、メディア、資料の有無を点数化し、単純な
語数ではなく推定作業量が均等になるよう割り当てます。

## バックアップ

管理画面のバックアップボタン、または次を定期実行します。

```bash
python3 scripts/backup_v2.py
```

最低でも毎日1回と、大量インポート・本番切替の直前に実行します。バックアップは
サーバーとは別の暗号化された保存先にも複製してください。

## 本番起動

環境変数を読み込んだうえで、開発サーバーではなくGunicornを使います。

```bash
./v6/venv/bin/gunicorn -c gunicorn.conf.py wsgi:app
```

Gunicornの前段にHTTPS対応のリバースプロキシを置きます。`YONAGUNI_HTTPS=1` と
`YONAGUNI_TRUSTED_HOSTS` は本番で必須です。

### Render

Renderではリポジトリ直下の `render.yaml` からBlueprintを作成します。SQLite、
アップロード媒体、バックアップは `/var/data` の永続ディスクに保存します。
Renderの初回作成画面で管理者ユーザー名・表示名・12文字以上の初期パスワードを
秘密環境変数として設定してください。秘密鍵はRenderが自動生成します。

`database/yonaguni_v2_seed.db` は公開辞書の初期データ専用です。ユーザー、監査履歴、
ログイン試行、点検依頼は含みません。本番運用DBをGitへ追加してはいけません。

## リリース前検査

```bash
./v6/venv/bin/python -m unittest discover -s tests -v
python3 scripts/verify_release.py
```

`integrity: ok`、`foreign_key_violations: 0`、テスト成功を確認してから切り替えます。

## 切り戻し

1. v2への書き込みを停止する。
2. 障害発生直前の検証済みバックアップを別名でコピーする。
3. `YONAGUNI_DATABASE` をそのコピーへ向ける。
4. 整合性検査と検索索引再構築を実行する。
5. 起動後、公開検索と管理ログインを確認する。

元の `v6/database/yonaguni_dict.db` は移行完了が確定するまで変更・削除しません。
